/**
 * Re-export of the AlgoKit-generated ARC-56 AppClient.
 *
 * This is intentionally kept as a separate barrel so the handwritten SDK can
 * import from one stable path while the generator output file name stays fixed.
 */

export * from './AsaMetadataRegistry'
