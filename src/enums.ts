/**
 * Copy of ASA Metadata Registry smart contract enums (ported from Python).
 */

// ---------------------------------------------------------------------------
// Enums
// ---------------------------------------------------------------------------
export const MBR_DELTA_NULL = 0 as const
export const MBR_DELTA_POS = 1 as const
export const MBR_DELTA_NEG = 255 as const
export const JSON_KEY_TYPE_STRING = 0 as const
export const JSON_KEY_TYPE_UINT64 = 1 as const
export const JSON_KEY_TYPE_OBJECT = 2 as const
export const B64_URL_ENCODING = 0 as const
export const B64_STD_ENCODING = 1 as const
